DATABASE_URL="postgresql://devport:devport@localhost:5432/devport"

BETTER_AUTH_SECRET="replace-with-at-least-32-characters-secret"
# Origin the browser uses for auth (user app when Next proxies /api to the backend). Not the backend port alone.
BETTER_AUTH_URL="http://localhost:3000"

# Optional: override backend URL for Next.js rewrites (defaults to http://127.0.0.1:3001).
# BACKEND_URL="http://127.0.0.1:3001"

# Server-side portfolio fetch (defaults to http://127.0.0.1:3001).
# API_URL="http://127.0.0.1:3001"

GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
GITHUB_CLIENT_ID=""
GITHUB_CLIENT_SECRET=""

AWS_ACCESS_KEY_ID=""
AWS_SECRET_ACCESS_KEY=""
AWS_S3_BUCKET=""
AWS_REGION="us-east-1"

GITHUB_API_KEY=""

NEXT_PUBLIC_API_URL="http://localhost:3001"
# Used by the portfolio app contact form (browser → API; must match CORS / trusted origins).
NEXT_PUBLIC_APP_URL="http://localhost:3000"
NEXT_PUBLIC_DASHBOARD_URL="http://localhost:3003"
NEXT_PUBLIC_PORTFOLIO_URL="http://localhost:3002"
NEXT_PUBLIC_WEBSITE_URL="http://localhost:3004"
# Optional: marketing links (website app)
# NEXT_PUBLIC_GITHUB_REPO_URL="https://github.com/your-org/devport"
