# Legacy portfolio (pre-DevPort)

The original **Next.js 12** Pages Router portfolio was moved here as `legacy/original-portfolio/`
when the **DevPort** Turborepo was created.

Use it as the source when migrating the VS Code template into
`apps/portfolio/src/templates/vscode/` (see `plan/12-vscode-template.md`).

Do not delete this folder until the template migration is complete.
